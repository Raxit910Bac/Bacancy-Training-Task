// Task 3 - IIFE (Immediately Invoked Function Expression)

// Create simple function that will run only once while we start program.

(function () {
  console.log("This function runs only once at the start!");
})();


// Notes: 
// - This function executes immediately when the program starts.
// - It does not get called again because it's not assigned to any variable.
